# 教师听课评课管理系统 PRD

## 项目概述
- **项目背景与目标**：为学校教学管理提供数字化工具，规范听课评课流程，沉淀教学数据，促进教师专业成长与教学质量提升。目标是完成一个覆盖核心业务流程的可运行Web应用原型。
- **目标用户群体**：学校教学管理人员（管理员）、听课教师（评价人）、被听课教师（被评价人）。
- **核心价值定位**：实现听课评课流程的线上化、标准化与数据化，提升管理效率，提供客观的教学评估依据。

## 功能需求
- **功能模块划分**：
    - 用户认证与权限管理模块
    - 听课计划管理模块
    - 课堂评价表填写与提交模块
    - 评课记录查询与归档模块
    - 统计分析与报表模块
- **详细功能列表**：
    - 用户登录/登出，基于角色的权限控制（管理员、教师）。
    - 管理员创建、发布、管理听课计划（含时间、班级、科目、听课人）。
    - 听课教师按计划进入课堂，填写结构化评价表（含多维度评分、文字评语）。
    - 提交评价表，状态流转为“已提交”，支持查看本人提交记录。
    - 管理员归档评课记录，支持按多条件（教师、时间、课程等）查询。
    - 系统自动生成统计图表：教师评分趋势图、课程评价对比图、评价维度分布图。
- **功能优先级排序**：
    - P0：用户登录、权限控制、评价表填写与提交、基础查询。
    - P1：听课计划管理、评课记录归档、统计图表生成。
    - P2：数据导出、高级筛选、通知提醒。
- **功能依赖关系**：
    - 评价表填写依赖听课计划的创建与发布。
    - 统计分析依赖已提交的评价数据。
    - 所有核心功能均依赖用户认证与权限验证。

## 技术建议
- **技术栈选型理由**：
    - **前端**：Vue 3 + Element Plus，生态成熟，开发高效，UI组件丰富，适合构建管理后台。
    - **后端**：Node.js + Express，前后端语言统一，开发效率高，易于构建RESTful API。
    - **数据库**：MySQL，关系型数据库，适合存储结构化的计划、评价、用户数据，保证数据一致性。
    - **部署**：Nginx作为反向代理和静态资源服务器，提高应用访问性能与稳定性。
- **架构设计建议**：采用前后端分离架构。前端为单页面应用（SPA），后端提供API接口。分层设计：Controller处理请求，Service实现业务逻辑，Model负责数据交互。
- **关键技术决策**：
    - 使用JWT（JSON Web Token）实现无状态身份认证与授权。
    - 数据库设计需包含用户表、角色表、计划表、评价表，并建立合理关联。
    - 前端路由采用Vue Router，状态管理可采用Pinia。

## 验收标准
- **功能验收标准**：
    - 用户可使用测试账号成功登录，不同角色看到不同菜单。
    - 管理员可成功创建并发布一个听课计划。
    - 教师可按计划找到待听课项，并完整填写评价表后成功提交。
    - 在评课记录中，管理员可按条件查询到已提交的记录并执行归档操作。
    - 系统能正确生成并展示至少3类统计图表（如折线图、柱状图、饼图）。
- **性能验收标准**：
    - 页面首次加载时间在普通网络环境下小于3秒。
    - API接口响应时间（95%）小于500毫秒。
    - 支持至少50个并发用户进行基本操作。
- **安全验收标准**：
    - 用户密码需经哈希加密存储。
    - API接口需进行权限校验，无权限用户无法访问。
    - 防止常见的Web安全漏洞（如SQL注入、XSS）。

## 软件工程(SE)领域特定内容
- **功能流程**：
    - **核心业务流程图**：计划创建 -> 计划发布 -> 教师听课并填写评价表 -> 提交评价 -> 管理员归档 -> 数据统计分析。
    - **用户操作流程**：登录 -> 选择角色对应功能 -> 执行操作（增删改查）-> 查看结果/反馈。
    - **异常处理流程**：表单提交失败时给出明确错误提示；接口请求失败时进行友好提示并记录日志。
- **用户交互设计**：
    - **交互设计原则**：清晰、一致、高效、反馈及时。使用Element Plus组件库保持风格统一。
    - **关键界面设计要点**：评价表单需分维度、分条目，支持评分和文本输入；列表页需提供清晰的筛选条件和分页。
    - **用户体验优化建议**：操作成功/失败给予Toast提示；重要操作（如提交、删除）增加二次确认；加载状态使用骨架屏或Loading动画。
- **界面原型建议**：
    - **核心页面布局建议**：采用经典后台布局：左侧固定导航菜单，顶部为用户信息栏，中间主内容区。
    - **组件设计规范**：遵循Element Plus官方文档的组件使用规范，确保样式、交互一致。自定义组件需封装并文档化。