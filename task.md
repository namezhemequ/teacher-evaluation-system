# 教师听课评课管理系统 - 开发任务清单

## 任务清单概述
根据项目需求文档、技术文档和 API 契约，拆解为以下10个可执行的开发任务。每个任务独立完成，有明确的验收标准。

## T-01: 项目初始化与基础架构搭建

**类型**: 部署/后端/前端
**优先级**: P0
**依赖**: 无
**预估工作量**: 中
**描述**: 初始化前后端项目，配置开发环境，搭建基础代码框架。包括Vue 3 + Element Plus前端项目、Node.js + Express后端项目、MySQL数据库初始化脚本，以及Nginx反向代理基础配置。
**验收标准**:
- [ ] 前后端项目可分别启动，无报错。
- [ ] 前端可访问登录页，后端可响应基础健康检查API（如 `/api/health`）。
- [ ] 数据库连接配置正确，可成功连接。
- [ ] 项目目录结构清晰，符合推荐技术栈规范。

## T-02: 数据库设计与初始化

**类型**: 数据库
**优先级**: P0
**依赖**: T-01
**预估工作量**: 中
**描述**: 根据PRD和技术文档设计并创建核心数据表，包括用户表、角色表、听课计划表、评价维度表。编写初始化脚本，插入默认角色和测试管理员账号。
**验收标准**:
- [ ] 所有核心表（users, roles, plans, evaluations, dimensions）创建成功。
- [ ] 表间外键关系正确建立。
- [ ] 初始化脚本成功运行，默认管理员账号可正常登录。
- [ ] 数据库ER图可清晰展示实体关系。

## T-03: 用户认证与授权模块

**类型**: 后端
**优先级**: P0
**依赖**: T-02
**预估工作量**: 大
**描述**: 实现用户登录、登出、Token刷新接口。基于JWT实现认证中间件，实现基于角色的权限控制（管理员、教师）。密码需使用bcrypt哈希存储。
**验收标准**:
- [ ] 提供 `/api/v1/auth/login` 接口，成功返回Token和用户信息。
- [ ] 提供 `/api/v1/auth/profile` 接口，需携带有效Token访问。
- [ ] 未登录或Token无效时，受保护接口返回401状态码。
- [ ] 非管理员角色访问 `/api/v1/plans` 等管理接口时返回403状态码。

## T-04: 前端基础布局与路由守卫

**类型**: 前端
**优先级**: P0
**依赖**: T-01, T-03
**预估工作量**: 中
**描述**: 实现应用主布局（侧边栏、顶栏、内容区）。配置Vue Router，实现全局路由守卫，根据用户角色动态生成侧边菜单，未登录用户重定向至登录页。
**验收标准**:
- [ ] 成功渲染包含侧边导航和顶栏的主布局。
- [ ] 未登录时访问任何页面均跳转至 `/login`。
- [ ] 登录后，管理员和教师角色看到不同的侧边菜单项。
- [ ] 刷新页面后，用户登录状态和当前路由保持。

## T-05: 听课计划管理模块（后端API）

**类型**: 后端
**优先级**: P1
**依赖**: T-03
**预估工作量**: 大
**描述**: 实现听课计划的增删改查（CRUD）及状态流转（草稿->发布）API。包括创建计划、发布计划、获取计划列表（分页、筛选）、更新计划、删除计划。
**验收标准**:
- [ ] 管理员可通过 `POST /api/v1/plans` 成功创建计划，状态为“草稿”。
- [ ] 可通过 `PUT /api/v1/plans/{id}/publish` 将计划状态更新为“已发布”。
- [ ] `GET /api/v1/plans` 接口支持按状态、教师ID筛选，并正确返回分页数据。
- [ ] 非管理员调用创建、发布、删除接口时返回403。

## T-06: 评价表填写与提交模块

**类型**: 后端/前端
**优先级**: P0
**依赖**: T-05
**预估工作量**: 大
**描述**: 后端提供教师提交评价表的API。前端实现评价表单页面，包含多维度评分输入（如教学目标、教学内容、教学方法等）和总体评语文本框。支持从计划列表进入待听课项进行评价。
**验收标准**:
- [ ] 教师可通过 `POST /api/v1/plans/{planId}/evaluations` 提交评价，包含维度评分和总体评语。
- [ ] 提交后，评价状态变为“已提交”，且同一教师对同一计划只能提交一次。
- [ ] 前端评价表单界面清晰，评分输入控件（如滑块、评分条）可用。
- [ ] 提交成功后有明确提示，并可查看已提交的评价详情。

## T-07: 评课记录查询与归档模块

**类型**: 后端/前端
**优先级**: P1
**依赖**: T-06
**预估工作量**: 中
**描述**: 后端实现管理员查询所有评课记录（多条件组合筛选）及归档记录的API。前端实现记录列表页，支持按教师、课程、时间范围、状态筛选，并提供归档操作按钮。
**验收标准**:
- [ ] `GET /api/v1/records` 接口支持按教师ID、学科、日期范围、状态进行组合查询。
- [ ] 管理员可通过 `PUT /api/v1/records/{id}/archive` 将记录状态更新为“已归档”。
- [ ] 前端列表页正确展示筛选条件，分页功能正常。
- [ ] 归档操作有二次确认提示，操作成功后列表状态实时更新。

## T-08: 统计分析与图表展示

**类型**: 后端/前端
**优先级**: P1
**依赖**: T-06
**预估工作量**: 大
**描述**: 后端提供三个统计分析数据接口（教师评分趋势、课程评价对比、评价维度分布）。前端使用ECharts等图表库，在统计页面可视化展示至少三类图表。
**验收标准**:
- [ ] 后端 `GET /api/v1/statistics/trend` 等三个接口能正确返回格式化数据。
- [ ] 前端统计页面成功集成并展示折线图（趋势）、柱状图（对比）、饼图（分布）。
- [ ] 图表支持按时间范围、教师、学科等参数动态刷新数据。
- [ ] 图表加载状态和空数据状态有友好提示。

## T-09: 核心业务流程集成测试

**类型**: 测试
**优先级**: P2
**依赖**: T-05, T-06, T-07
**预估工作量**: 中
**描述**: 编写端到端测试用例，覆盖核心业务流程：管理员登录->创建并发布计划->教师登录->查看计划并填写提交评价->管理员登录->查询记录并归档->查看统计图表。
**验收标准**:
- [ ] 测试用例覆盖所有P0、P1功能点。
- [ ] 核心业务流程（计划->评价->归档->统计）可走通，无阻塞性Bug。
- [ ] 权限控制测试通过，角色越权操作被正确阻止。
- [ ] 测试报告输出，记录通过率与关键缺陷。

## T-10: 应用部署与文档完善

**类型**: 部署/测试
**优先级**: P2
**依赖**: T-09
**预估工作量**: 小
**描述**: 编写生产环境部署脚本或Docker配置。完善项目README、API接口文档（基于现有契约）、用户操作手册。进行最后的性能与安全检查。
**验收标准**:
- [ ] 提供可一键部署的脚本或Docker Compose文件。
- [ ] API文档与代码实际接口保持一致。
- [ ] 系统在模拟50并发用户下，核心API响应时间小于500ms。
- [ ] 密码已哈希存储，前端无XSS漏洞，后端接口有基础参数校验。

---
**任务依赖关系图**:
T-01 → T-02 → T-03 → T-04
T-03 → T-05 → T-06 → T-07
T-06 → T-08
T-05, T-06, T-07 → T-09 → T-10